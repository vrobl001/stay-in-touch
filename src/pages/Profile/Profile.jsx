import React from 'react';
import styles from './Profile.module.css';

const Profile = (props) => {
  return (
    <main>
      <div>
        <h1>Profile Page</h1>
      </div>
    </main>
  );
};

export default Profile;
