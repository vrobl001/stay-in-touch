import React from 'react';
import styles from './Login.module.css';

const Login = (props) => {
  return (
    <main>
      <div>
        <h1>Login Page</h1>
      </div>
    </main>
  );
};

export default Login;
